name: 'tpch_medallion_snowflake'
version: '1.0.0'
config-version: 2

profile: 'tpch_medallion_snowflake'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  tpch_medallion_snowflake:
    +materialized: table
    
    bronze:
      +materialized: table
      +schema: bronze
      +tags: ["bronze", "raw", "snowflake"]
      
    silver:
      +materialized: table  
      +schema: silver
      +tags: ["silver", "cleaned", "snowflake"]
      
    gold:
      +materialized: table
      +schema: gold
      +tags: ["gold", "business", "snowflake"]
